# Requirements Note

## ⚠️ Important: Use Main Requirements File

All dependencies for this project are now consolidated in:
```
../requirements.txt
```

## Installation

From the root directory:
```bash
cd /Users/sujithjulakanti/Desktop/Datathon
python3 -m pip install -r requirements.txt
```

## LinkedIn-Specific Setup

After installing requirements, install Playwright browsers:
```bash
playwright install chromium
```

## Old Requirements File

The old `requirements.txt` has been backed up as `requirements.txt.bak` for reference.
